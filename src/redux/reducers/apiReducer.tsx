import {} from '../actions/ActionList';

const initialState = {};

function apiReducer(state = initialState, action: { type: any }) {
  switch (action.type) {
    default:
      return state;
  }
}

export default apiReducer;
